海伦公式 
S = sqrt(p(p-a)(p-b)(b-c)) 
p = (a+b+c)/2  
a, b, c 为三角形三边长

基姆拉尔森公式：
if (m == 1 || m == 2) m += 12, y --;
w = (d + 2*m + 3*(m+1)/5 + y + y/4 - y/100 + y/400) % 7
ans(w) = {星期一，星期二，星期三，星期四，星期五，星期六，星期天}
y/m/d 为年月日

威尔逊定理：
(p-1)! ≡ p-1 (mod p), p为质数

欧拉定理：
a^x % p = {
    if gcd(a, p) == 1:
        a ^ (x%phi(p))
    if gcd(a, p) != 1:
        if x < phi(p):
            a ^ x
        if x >= phi(p):
            a ^ (x%phi(p) + phi(p))
}